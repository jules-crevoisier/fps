branch: feature/aaa-roadmap (commit/push at validated steps, never main)
state 2026-09-26 (after full reset, commit a940b95, pushed):
- minimal prototype: boot straight into TDM 4v4 vs bots on Shipment (scenes/levels/maps/shipment.tscn, editable scene),
  Ravage only, Verrou only, no abilities, UI = crosshair + hit marker; players face map center at spawn.
- kept tools: tools/ai3d/bridge_autoexport.py, tools/blender/ai_import_painted.py (+lib), licence_check, test.sh,
  review/gameplay_probe.gd + report.py (CI), bake_bot_spots.gd, cosmetic_lint.gd (tests use them).
- deleted: Wasteland + generator + plan tools, FP v2, 6 weapons, docs/, tasks/ (backlog), reports content, research.
- previous full state: git tag avant-nettoyage-2026-09-26 (pushed). Stashes 'wip arrete' still in git stash list (obsolete).
user direction: user makes maps and models; Claude focuses on gameplay code (movement, shooting feel, bots, net) and
integration. Usage budget: one agent at a time, Sonnet default, small steps.
game-feel audit findings (doc deleted, summary): gun has no punch (viewmodel kick 0.04°, shake 0.01°, 12 cm muzzle
flash, 1 px tracer); recoil pattern erased by aim recovery (26 % of designed climb) and applied at 60 Hz; auto fire
snaps to physics ticks; ADS zoom/gun out of sync, scoped sens not scaled; audio: per-layer pitch drift, no impacts/
whizz/low-ammo. Still valid for the Ravage; recover full doc from tag if needed (docs/research/13_game_feel.md).
next: wait for user feedback / models.
- 1e02ea6: quick start 1v1 (team_size=1). NEW: visual identity (Spider-Verse, saturated/contrasted, halftone shadows), shared Godot+Blender shader, then UI + spells. WAITING user reference images.
- USER: style = Borderlands x Marvel Rivals (+ halftone shadows, saturated). Lead wrote art/style/toon_style.json (single source). ONE sonnet builder: toon_bd.gdshader + outline + inner lines + ToonStyle.gd (Godot), toon_bd_nodes.py + add-on (Blender), parity scene both engines + compare, apply to Verrou/Ravage/Shipment, art/style/README.md. Review parity images before showing user. Then UI + spells.
- USER: outlines are NOT thick (BL4 screenshot), 'do real research', 'tirer vraiment sur Borderlands'. Research: BL = hand-inked textures + thin post-process Sobel outline (depth+normals) + sharpened real lighting + saturated grading; no halftone. toon_style.json v2 written. ONE sonnet builder relaunched (toon_bd shader + Sobel post-process + ToonStyle.gd, Blender node group + Line Art + add-on, INK BAKE tool for textures, parity, captures). Review before showing user.
- User exported frog cowboy (Tripo, rigged Mixamo 65 bones incl. fingers, 14.4k tris, NO texture (grey 0.8), NO animations) -> copied to assets/incoming/tripo/frog_cowboy_rigged_notex.glb. Asked user for a textured export. Plan: Godot retarget (BoneMap SkeletonProfileHumanoid) so UAL clips play on it; weapon attach to RightHand; toon + ink bake; editable character + viewmodel scenes for the user. Chrome extension disconnected.
- Blender paint help: brush fixed (size pressure off in frog_cowboy_paint.blend). 2D fill-per-island: art/characters/frog_cowboy/make_uv_zones.py -> frog_texture.png (103 UV islands, flat green on dark, separated) ; user uses Image>Replace + Fill tool in 2D. Shader agent a1e138a59aefe72ea still running.
- Shader agent done (uncommitted). Lead fixes: toon_bd ALPHA write removed (was transparent -> no depth/normals -> no outline/shadows), light() /PI + ratio vs albedo + rim/spec to SPECULAR_LIGHT, Blender Hue 0.5+deg/360, Blender world sky_blue camera/ambient lighting, sun_energy 1.0. Parity 0.45 -> 0.197 (target 0.06: Blender emission ignores sun energy + glossy, frog texture colours differ). Tests rendering+player 479 green. Ink bake too dark, not wired. NOT committed: waiting user visual OK.
- 2026-09-26 soir: user painted frog (Untitled.blend in art/characters/frog_cowboy). export_frog.py (bg Blender, UV edge padding via tools/blender/lib/uv_mask.py) -> assets/models/characters/frog_cowboy.glb + frog_cowboy_albedo.png. ONE sonnet builder running: retarget Mixamo->humanoid, replace Verrou, WeaponSocket in scenes/characters/frog_cowboy.tscn, README guide, tests, captures reports/checkpoints/2026-09-26_frog_cowboy/. NEXT: review images; then CLEANUP step (user asked to remove all legacy files: old characters, Cartoon.gd textures, agent profiles...) with reference check + tests; nothing deleted yet. Shader fixes still uncommitted.
- Builder DONE (uncommitted): first export had no armature at all (Blender hidden-object export bug), user re-exported -> real Mixamo rig (65 bones) confirmed. Retarget built by hand (Godot's own BoneMap import UI never touched, format not needed): scripts/import/HumanoidBoneMap.gd (Mixamo + UAL DEF-* -> 52 shared humanoid names) + scripts/import/FrogCowboyPostImport.gd (EditorScenePostImport, renames frog's skeleton+skin binds at import) + tools/rigging/bake_frog_animations.gd (offline, retargets 19 needed clips from assets/incoming/quaternius/ual.glb by rest-delta math, writes resources/agents/anim/frog_cowboy_humanoid.tres) — verrou.glb/ual.glb untouched, zero shared-pipeline risk. CharacterBody.MODEL_OVERRIDE routes agent "verrou" -> scenes/characters/frog_cowboy.tscn (AgentDatabase/tokens.json colors left alone, only the 3D body switched). ThirdPersonWeapon.gd now prefers a "WeaponSocket" node from the character over its old hard-coded DEF-hand.R bone. Tests: +8 (frog_cowboy_character) +4 (humanoid_bone_map) +1 (player_look frog_cowboy_tex) all green; full suite 1433 cases/0 failures (18 pre-existing orphans in test_ping/test_comic_tokens, not mine). Screenshots reviewed+iterated (weapon rotation calibrated by trial, 3 renders look right, no T-pose/explosion). KNOWN GAPS (flagged, not fixed): CharacterAnimator.gd's head-squash/flinch/upper-body-filter bones are still DEF-* (global consts, out of this task's file list) so those cosmetic layers silently no-op for frog; tools/test.sh's licence gate currently fails on a pre-existing untracked ravage_inked.glb (unrelated to this task). Cleanup step (deleting legacy characters) still not started.
- Frog integrated by agent (CharacterBody MODEL_OVERRIDE verrou->frog_cowboy, FrogCowboyPostImport, bake_frog_animations.gd -> resources/agents/anim/frog_cowboy_humanoid.tres, scenes/characters/frog_cowboy.tscn WeaponSocket, README). Lead: team rim moved to EMISSION thin band (toon_bd). Tests rendering+player 492 green; agent full suite 1433 green (tools/test.sh licence gate fails on ravage_inked.glb provenance). Visual issues: run clip raises left arm (retarget arms), rifle held low/sideways (pistol clips), red poncho reads magenta in shadow. Windowed Godot: --screen 1. Nothing committed.
- 2026-09-26 nuit TASK1 frog 100% animated (user: one task at a time; anims made by lead; FP = frog arms). Lead authored clips by script: art/characters/frog_cowboy/anim/{rigkit,clips,rifle,extra,fp,library,preview}.py; export_frog.py (19 clips + WeaponGrip) / export_frog_fp.py (arms-only mesh, FPCamera+WeaponGrip, 7 FP_* clips). Bone axes: Godot keeps Blender local axes -> WeaponGrip/FPCamera built with Y=up, Z=back (=> -Z forward in Godot). 3P agent done (native clips, post-import track rename, rig profile, counter-scale). Lead fixes: upper_body_blend_for (two-handed keeps 1.0 in jog/sprint), enemy rest rim 0 + no team hull on BD materials (magenta blotches), shadow hue -8. Ravage recolored (art/weapons/ravage/recolor_ravage.py: gunmetal/walnut/brass, Muzzle/Foregrip fixed). FP integration agent RUNNING (ViewModel). Open: enemy readability (idea: stencil-based colored outline, Godot 4.5+). Nothing committed.
- CHECKPOINT (user warned of interruption). NEXT if resumed: 1) read FP agent report (ViewModel frog arms, captures reports/checkpoints/2026-09-26_frog_fp_ingame/), review images, tune art/characters/frog_cowboy/anim/fp.py IDLE/low/run offsets then re-run export_frog_fp.py; 2) full gdUnit suite; 3) fix licence gate (ravage_inked.glb provenance or delete inked assets) ; 4) commit task 1 on feature/aaa-roadmap after user OK; 5) then task 2 = small gun (Tripo in Chrome: image->3D, Smart UV, segment, save, export) ; enemy stencil outline later. Re-export commands: blender -b art/characters/frog_cowboy/Untitled.blend -P art/characters/frog_cowboy/export_frog.py ; same with export_frog_fp.py [-- --preview dir]; then godot --headless --path . --import.
- TASK1 near done: FP arms wired by agent (FPArmsRig.gd, FPArmsMath.gd, ViewModel arms path, inspect=E since F=pickup). Lead: FP framing retuned (fp.py IDLE/ads/low), thumbs + wrist twist fixed (rigkit), probe tool tools/rigging/probe_fp_clips.gd, Weapon._spawn_tracer null-safe, anchor test -> geometric check, inked test assets moved to scratchpad (inked_backup). Full suite via GODOT_BIN=... bash tools/test.sh: 1494/0 fail, licence OK. WAITING user in-game test + OK to commit/push.
- FP polish round 3 (user feedback): weapon_changed emitted every shot -> draw retriggered (guarded in ViewModel); ADS via FP_ADS_In seek (TimeScale0+TimeSeek) instead of blendspace; ADS pose under crosshair (HANDLE_TOP anchor); grip R_WRIST moved back/down; BOLT charging from left side + detour keys; Rig forces 30 fps (was 24 -> clips 25% long); FP_Idle loop; CharacterAnimator reload uses real clip length; trigger index curl 62; tools: probe_fp_clips.gd, fp_filmstrip.gd (Engine.max_fps=60) + filmstrip_sheet.py. Player tests 333 green.
- FP polish round 4: ADS 1-frame 45deg flash + 'arm goes crazy' = twist wrap in rigkit.spread_twist (atan2 +/-180 -> 145deg forearm jumps in ADS_In/Reload/Inspect/Draw); fixed with per-clip unwrap memory. Bolt pull removed (hand through gun) -> mag slap + left-side return (FP+3P). IdleAds Blend2 sync=true. tools/rigging/fp_glitch_detect.gd: 0 flagged / worst 6.3deg over 10 aim cycles x2.
